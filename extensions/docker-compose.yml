# J.A.R.V.I.S. - Production stack (Raspberry Pi)
# Self-contained services for offline operation.

networks:
  jarvis_net:
    driver: bridge

services:
  jarvis-ollama:
    image: ollama/ollama:latest
    container_name: jarvis_ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - /data/jarvis/ollama:/root/.ollama
    environment:
      - OLLAMA_HOST=0.0.0.0
      - OLLAMA_CONTEXT_LENGTH=4096
      - OLLAMA_NUM_PARALLEL=1
      - OLLAMA_MAX_LOADED_MODELS=1
    networks:
      - jarvis_net

  jarvis-qdrant:
    image: qdrant/qdrant:v1.16
    container_name: jarvis_qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - /data/jarvis/qdrant:/qdrant/storage
    networks:
      - jarvis_net

  jarvis-memory:
    build: ./memory
    container_name: jarvis-memory
    restart: unless-stopped
    ports:
      - "8401:8401"
    volumes:
      - /data/jarvis/memory:/app/data
    networks:
      - jarvis_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8401/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  jarvis-voice:
    build: ./voice
    container_name: jarvis-voice
    restart: unless-stopped
    ports:
      - "8402:8402"
    volumes:
      - /data/jarvis/voice/models:/app/models
      - /data/jarvis/voice/audio:/app/audio
    devices:
      - /dev/snd:/dev/snd
    networks:
      - jarvis_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8402/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  jarvis-vision:
    build: ./vision
    container_name: jarvis-vision
    restart: unless-stopped
    ports:
      - "8405:8405"
    environment:
      - VISION_CAMERA_INDEX=0
    volumes:
      - /data/jarvis/vision:/app/data
    devices:
      - /dev/video0:/dev/video0
    networks:
      - jarvis_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8405/health"]
      interval: 30s
      timeout: 8s
      retries: 3

  jarvis-brain:
    build: ./brain
    container_name: jarvis-brain
    restart: unless-stopped
    ports:
      - "8403:8403"
    environment:
      - OLLAMA_URL=http://jarvis_ollama:11434
      - QDRANT_URL=http://jarvis_qdrant:6333
      - MEMORY_URL=http://jarvis-memory:8401
      - VOICE_URL=http://jarvis-voice:8402
      - VISION_URL=http://jarvis-vision:8405
      - MOTORS_URL=http://jarvis-motors:8404
      - MODEL_NAME=gemma3:1b
      - MODEL_REASONING=qwen2.5:3b
      - JARVIS_LANGUAGE=es
    depends_on:
      jarvis-memory:
        condition: service_healthy
      jarvis-voice:
        condition: service_healthy
      jarvis-vision:
        condition: service_healthy
    networks:
      - jarvis_net

  jarvis-kiwix:
    image: ghcr.io/kiwix/kiwix-serve:3.8.2
    container_name: jarvis_kiwix
    restart: unless-stopped
    ports:
      - "8500:8080"
    volumes:
      - /data/jarvis/zim:/data
    command: /data/*.zim
    networks:
      - jarvis_net

  # jarvis-motors:
  #   build: ./motors
  #   container_name: jarvis-motors
  #   restart: unless-stopped
  #   ports:
  #     - "8404:8404"
  #   devices:
  #     - /dev/gpiomem:/dev/gpiomem
  #   privileged: true
  #   networks:
  #     - jarvis_net
